<nav class="app-navbar">
    <div class="navbar-left">
        <button type="button" id="sidebarToggleBtn" class="btn btn-light d-lg-none p-2 rounded-3 border" aria-label="Toggle navigation">
            <i class="bi bi-list fs-5"></i>
        </button>
        <div class="navbar-context-title">
            <span class="d-none d-sm-inline">DentalCare Portal</span>
            <span class="text-muted d-none d-md-inline">&bull;</span>
            <span class="badge badge-dc badge-role-patient d-none d-md-inline">Clinical Edition</span>
        </div>
    </div>

    <div class="navbar-right">
        @if (User.Identity?.IsAuthenticated == true)
        {
            <!-- Help Tooltip -->
            <a href="#" class="btn-nav-icon text-decoration-none d-none d-sm-flex" title="Help & Documentation" onclick="alert('DentalCare Help: Refer to FRONTEND_SETUP.md for deployment and clinical workflows.'); return false;">
                <i class="bi bi-question-circle"></i>
            </a>

            <!-- Notification Bell -->
            <div class="dropdown">
                <button class="btn-nav-icon" type="button" id="notifDropdown" data-bs-toggle="dropdown" aria-expanded="false" title="Notifications">
                    <i class="bi bi-bell"></i>
                    <span class="nav-unread-dot"></span>
                </button>
                <ul class="dropdown-menu dropdown-menu-end shadow-sm border py-2" aria-labelledby="notifDropdown" style="min-width: 290px;">
                    <li class="px-3 py-2 border-bottom d-flex justify-content-between align-items-center">
                        <span class="fw-bold text-dark small text-uppercase" style="letter-spacing: 0.05em;">Notifications</span>
                        <span class="badge bg-light text-muted border small">0 new</span>
                    </li>
                    <li class="px-3 py-4 text-center text-muted small">
                        <i class="bi bi-bell-slash fs-4 d-block mb-1 text-secondary"></i>
                        No new notifications
                    </li>
                </ul>
            </div>
        }

        <!-- User Profile Menu -->
        <partial name="_UserMenu" />
    </div>
</nav>
